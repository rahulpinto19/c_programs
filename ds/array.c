#include<stdio.h>
#include<conio.h>
int main()
{
    int k;
    int arr[k];
    printf("enter the size of the array:");
    scanf("%d",&k);
    for(int i=0;i<k;i++)
    {
        scanf("%d",&arr[i]);
    }
}