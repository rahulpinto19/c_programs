#include<stdio.h>
#include<conio.h>
int main()
{
int pin;
    pin=1234;
    printf("\n\n enter the pin");
    scanf("%d",&pin);
    switch(pin)
    {
     case pin:
         printf("the available balance is 9000 ")
    }
}
